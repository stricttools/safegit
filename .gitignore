# Build output
/safegit
*.exe

# Go workspace: local development against an unreleased strictcli checkout
go.work
go.work.sum

# Added by rlsbl
node_modules/
__pycache__/
*.pyc
*.log
.DS_Store
coverage/
dist/
*.egg-info/
.rlsbl-notes-*.tmp
.credentials.json
.*-cache.json
.env
.env.local

# Added by rlsbl
*.local-only

# Selfdoc / Cloudflare
.wrangler/
# .stricttools/docs-state/manifest.json and .stricttools/docs-state/hashes/ are committed (fleet pattern):
# they are generated project state, not build output. Only the caches are local.
.stricttools/docs-cache/versions/
.stricttools/docs-state/data/

.rlsbl/lock
.claude/

# Benchmark temp files
.bench-*

.rlsbl-monorepo/lock

build/
target/

# Scratch space for git-behavior probes; guards skip it, contents are
# disposable (see experiments/README.md, the one committed file there)
/experiments/*
!/experiments/README.md
