{
  "schema_version": 2,
  "defaults": {
    "schema_version": 2,
    "app": {
      "env_prefix": null,
      "config": false,
      "config_format": "json",
      "config_path": null,
      "config_conflict_mode": "cli-wins",
      "proc_observe_allowlist": [],
      "global_flags": [],
      "commands": {},
      "groups": {},
      "deprecated": {},
      "tag_contracts": {},
      "checks": {},
      "config_fields": {},
      "infra": {}
    },
    "flag": {
      "short": null,
      "env": null,
      "env_separator": null,
      "prefixed": true,
      "choices": null,
      "elect_by": null,
      "unique": false,
      "conflict_mode": null,
      "negatable": null,
      "nullable": false
    },
    "arg": {
      "variadic": false,
      "choices": null
    },
    "choice": {
      "flags": []
    },
    "choice_record": {
      "help": null
    },
    "command": {
      "consequential": false,
      "dry_run_supported": true,
      "dry_run_unsupported_reason": null,
      "update_of": null,
      "write_mode": null,
      "payload_schema": null,
      "owns_stdout": false,
      "passthrough": false,
      "flags": [],
      "flag_sets": [],
      "args": [],
      "tags": [],
      "constraints": [],
      "hidden": false,
      "interactive": false,
      "config_fields": [],
      "grants": [],
      "forwarding": null
    },
    "group": {
      "commands": {},
      "groups": {},
      "deprecated": {},
      "tags": [],
      "hidden": false
    },
    "config_field": {
      "default": null,
      "bound_commands": []
    },
    "check": {
      "scope": null
    },
    "infra": {
      "roots": [],
      "handshakes": [],
      "connections": []
    }
  },
  "project_id": "github.com/smm-h/safegit",
  "name": "safegit",
  "version": "dev",
  "help": "concurrency-safe git wrapper for multi-agent use with atomic commits, oplog-based undo, and history rewriting",
  "proc_observe_allowlist": [
    [
      "git",
      "--no-optional-locks",
      "--version"
    ],
    [
      "git",
      "--no-optional-locks",
      "cat-file"
    ],
    [
      "git",
      "--no-optional-locks",
      "check-attr"
    ],
    [
      "git",
      "--no-optional-locks",
      "check-ignore"
    ],
    [
      "git",
      "--no-optional-locks",
      "diff"
    ],
    [
      "git",
      "--no-optional-locks",
      "diff-index"
    ],
    [
      "git",
      "--no-optional-locks",
      "diff-tree"
    ],
    [
      "git",
      "--no-optional-locks",
      "for-each-ref"
    ],
    [
      "git",
      "--no-optional-locks",
      "log"
    ],
    [
      "git",
      "--no-optional-locks",
      "ls-files"
    ],
    [
      "git",
      "--no-optional-locks",
      "ls-tree"
    ],
    [
      "git",
      "--no-optional-locks",
      "merge-base"
    ],
    [
      "git",
      "--no-optional-locks",
      "rev-list"
    ],
    [
      "git",
      "--no-optional-locks",
      "rev-parse"
    ],
    [
      "git",
      "--no-optional-locks",
      "status"
    ],
    [
      "git",
      "--no-optional-locks",
      "stripspace"
    ],
    [
      "git",
      "--no-optional-locks",
      "var"
    ]
  ],
  "global_flags": [
    {
      "name": "config-file",
      "help": "path to a custom safegit config file instead of the default location; when omitted the default location is used",
      "value_schema": {
        "type": "string"
      },
      "presence": "optional"
    }
  ],
  "commands": {
    "commit": {
      "name": "commit",
      "help": "stage and commit specified files in a single atomic operation",
      "effect": "mutating",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "attempts": {
            "type": "integer"
          },
          "dry_run": {
            "type": "boolean"
          },
          "execution_mode": {
            "type": [
              "string",
              "null"
            ]
          },
          "files": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "moved_records": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "id": {
                  "type": "string"
                },
                "new": {
                  "type": "string"
                },
                "old": {
                  "type": "string"
                },
                "origin": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "old",
                "new",
                "origin"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "moves_over_cap": {
            "type": "integer"
          },
          "old_sha": {
            "type": [
              "string",
              "null"
            ]
          },
          "parents": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "ref": {
            "type": "string"
          },
          "refused_moves": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "new": {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                "old": {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                "reason": {
                  "type": "string"
                }
              },
              "required": [
                "old",
                "new",
                "reason"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "residue": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "detail": {
                  "type": "string"
                },
                "step": {
                  "type": "string"
                }
              },
              "required": [
                "step",
                "detail"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "sha": {
            "type": [
              "string",
              "null"
            ]
          },
          "skipped_ignored": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "tree": {
            "type": "string"
          }
        },
        "required": [
          "ref",
          "parents",
          "tree",
          "sha",
          "old_sha",
          "files",
          "skipped_ignored",
          "attempts",
          "execution_mode",
          "residue",
          "dry_run",
          "moved_records",
          "refused_moves",
          "moves_over_cap"
        ],
        "type": "object"
      },
      "flags": [
        {
          "name": "m",
          "help": "commit message paragraph; repeating it joins the values with a blank line between them, so the first is the subject and the rest are the body",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "short": "m",
          "presence": "optional"
        },
        {
          "name": "F",
          "help": "read the full commit message body from a file instead of --m flags",
          "value_schema": {
            "type": "string"
          },
          "short": "F",
          "presence": "optional"
        },
        {
          "name": "branch",
          "help": "commit the staged files onto a different branch without switching to it",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "amend",
          "help": "amend the current HEAD commit by replacing it with updated content; omitted means a new commit",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "allow-empty",
          "help": "allow creating a commit even when no files have been changed; omitted means an empty commit is refused",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "allow-non-portable-targets",
          "help": "record a symlink whose target text will not resolve in another checkout -- an absolute target, or a relative one resolving outside the repository -- which the commit stores as the link text; omitted, and with --no-allow-non-portable-targets, such a link is refused with its target named, because elsewhere it resolves to nothing or to a file the repository never carried",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "trailer",
          "help": "add a key-value trailer line to the commit message (repeatable)",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "presence": "optional"
        },
        {
          "name": "hunks",
          "help": "commit only the selected hunks of one file, as 'path:1,3' or 'path:2-4' (the split is on the last colon, so a path containing colons stays intact); repeatable, once per path; omitted means every named file is committed whole",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "presence": "optional",
          "unique": true
        },
        {
          "name": "untrack",
          "help": "stop tracking a path, leaving the file itself on disk: the commit records its removal from the index (repeatable, one path each); the path must be tracked in the commit's parent; omitted means nothing is untracked",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "presence": "optional",
          "unique": true
        },
        {
          "name": "moved",
          "help": "declare that content moved, as 'old -> new' (repeatable, one pair each). End BOTH paths with a slash to declare a whole subtree. Quote a path C-style when it holds a space, a quote, a backslash or the arrow itself. The old path must be tracked in the commit's parent and gone from disk, and the new one must exist. A declaration also SUPPRESSES safegit's own reading of the delta for the paths it names, and supersedes a record safegit already minted for the same pair on the commit an --amend replaces. Omitted means the commit declares no moves of its own, and safegit still records the ones its delta witnesses on its own",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "presence": "optional",
          "unique": true
        },
        {
          "name": "moved-retract",
          "help": "retract a move record declared earlier in this branch's history, by its id -- the token a 'Moved:' trailer begins with (repeatable, one id each). The id must name a record that exists and is not already retracted in the history this commit is built on; one that does not is refused rather than written. To write an unchecked retraction, use --trailer 'Moved-Retract: <id>'. Omitted means the commit retracts nothing",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "presence": "optional",
          "unique": true
        }
      ],
      "args": [
        {
          "name": "files",
          "help": "files to commit, taken literally -- a colon in an argument is part of the filename, and hunk selection is --hunks",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "presence": "optional",
          "variadic": true
        }
      ],
      "tags": [
        "json"
      ],
      "grants": [
        {
          "name": "parent-bump",
          "reason": "committing in a submodule moves the parent's gitlink, so safegit commits the parent too when commit.autoBumpParent is on",
          "kind": "proc_mutate"
        }
      ]
    },
    "mv": {
      "name": "mv",
      "help": "move tracked paths and commit the moves with their records in one operation",
      "effect": "mutating",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "attempts": {
            "type": "integer"
          },
          "dry_run": {
            "type": "boolean"
          },
          "files": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "moved_records": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "id": {
                  "type": "string"
                },
                "new": {
                  "type": "string"
                },
                "old": {
                  "type": "string"
                },
                "origin": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "old",
                "new",
                "origin"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "parents": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "ref": {
            "type": "string"
          },
          "residue": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "detail": {
                  "type": "string"
                },
                "step": {
                  "type": "string"
                }
              },
              "required": [
                "step",
                "detail"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "sha": {
            "type": [
              "string",
              "null"
            ]
          },
          "tree": {
            "type": "string"
          }
        },
        "required": [
          "ref",
          "parents",
          "tree",
          "sha",
          "moved_records",
          "files",
          "attempts",
          "residue",
          "dry_run"
        ],
        "type": "object"
      },
      "flags": [
        {
          "name": "m",
          "help": "commit message paragraph; repeating it joins the values with a blank line between them, so the first is the subject and the rest are the body",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "short": "m",
          "presence": "required"
        },
        {
          "name": "create-missing-directories",
          "help": "make the destination's parent directories when they are not there, removing again what this invocation made if the move is rolled back; omitted, and with --no-create-missing-directories, a destination whose directory does not exist is refused and nothing is moved",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        }
      ],
      "args": [
        {
          "name": "pairs",
          "help": "one move each, written 'old -> new'. End BOTH paths with a slash to move a whole directory, which is recorded as ONE subtree record however many files it holds. Quote a path C-style when it holds a space, a quote, a backslash or the arrow itself. Every pair is checked before the first file is touched -- the source must be tracked and on disk, the destination must be free and its directory must already exist unless --create-missing-directories says otherwise, the source must carry no uncommitted content changes, and no two pairs may speak for the same path or chain into one another -- and a failure part-way through puts back everything already moved. The commit is the move and nothing else: each path is carried across as the blob its parent commit held, which is why a path with uncommitted content changes is refused rather than moved -- commit the content first and then move it, or move it on disk yourself and commit both at once with 'safegit commit --moved'",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "presence": "required",
          "variadic": true
        }
      ],
      "tags": [
        "json"
      ],
      "grants": [
        {
          "name": "parent-bump",
          "reason": "moving paths in a submodule moves the parent's gitlink, so safegit commits the parent too when commit.autoBumpParent is on",
          "kind": "proc_mutate"
        }
      ]
    },
    "merge-continue": {
      "name": "merge-continue",
      "help": "conclude a merge git stopped before committing. Every conflicted path is named with --resolve (or in a --resolve-file), and safegit writes the merge commit itself: HEAD plus the MERGE_HEAD line as parents; the merge's whole staged result as its tree, so a path the merge staged cleanly is never dropped; git's own message draft with its comment block stripped, or -m; the repository's commit-msg hook run and safegit's trailers injected; and the merge's whole state-file set removed afterwards, so a later commit is not refused. An empty merge needs no flag -- a merge commit records its parents whether or not the tree changed. Two merge shapes only raw git can start are REFUSED, each naming git's own 'merge --continue' and '--abort': an OCTOPUS, because every check safegit makes over a merge is written against two sides, and a content conflict git recorded with no AUTO_MERGE, which is a non-default strategy's signature and leaves the marker verification nothing to read",
      "effect": "mutating",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "attempts": {
            "type": "integer"
          },
          "autostash": {
            "additionalProperties": false,
            "properties": {
              "stash": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "state": {
                "type": "string"
              }
            },
            "required": [
              "state",
              "stash"
            ],
            "type": "object"
          },
          "declined_checks": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "check": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "reason": {
                  "type": "string"
                }
              },
              "required": [
                "check",
                "path",
                "reason"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "dry_run": {
            "type": "boolean"
          },
          "files": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "operation": {
            "type": "string"
          },
          "parents": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "ref": {
            "type": "string"
          },
          "residue": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "detail": {
                  "type": "string"
                },
                "step": {
                  "type": "string"
                }
              },
              "required": [
                "step",
                "detail"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "resolutions": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "choice": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "source": {
                  "type": "string"
                }
              },
              "required": [
                "path",
                "choice",
                "source"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "sha": {
            "type": [
              "string",
              "null"
            ]
          },
          "state_cleared": {
            "type": "boolean"
          },
          "tree": {
            "type": "string"
          }
        },
        "required": [
          "operation",
          "ref",
          "sha",
          "parents",
          "tree",
          "files",
          "resolutions",
          "state_cleared",
          "attempts",
          "declined_checks",
          "autostash",
          "residue",
          "dry_run"
        ],
        "type": "object"
      },
      "flags": [
        {
          "name": "resolve",
          "help": "resolve one conflicted path, as 'path=ours|theirs|worktree|delete' (repeatable, once per path; the split is on the last '=', so a path containing one stays intact). The keywords are defined by INDEX STAGE: ours is the stage-2 blob -- the content this branch already had, before the merge; theirs is the stage-3 blob -- the content merged in from the other side; worktree is the file's current content on disk; delete leaves the path out of the commit. ours and theirs also WRITE the chosen content into the working tree (git's own checkout --ours), and delete removes the file from disk (git's own rm). Paths are repository-relative. Omitted, every conflicted path is unresolved, which is a refusal listing them",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "presence": "optional"
        },
        {
          "name": "resolve-file",
          "help": "read resolutions from a TOML file of [[resolutions]] tables, each with a path and a choice key; combinable with --resolve, and a path named by both is a hard error; omitted means the --resolve flags are the whole declaration",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "m",
          "help": "commit message paragraph, replacing git's own draft; repeating it joins the values with a blank line between them; omitted means git's draft for this operation with its comment block stripped",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "short": "m",
          "presence": "optional"
        },
        {
          "name": "trailer",
          "help": "add a key-value trailer line to the commit message (repeatable)",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "presence": "optional"
        },
        {
          "name": "discard-unmatched-worktree",
          "help": "destroy working-tree content that matches no side of the conflict. A resolution that writes a stage over the file on disk, or deletes it, is refused when what is there is neither an index stage nor the content git itself wrote -- that is a hand edit, held in no commit, no stage and no stash. Passing this elects the destruction and names each file it takes. Omitted, and with --no-discard-unmatched-worktree, such a path is refused and nothing is committed; resolve it to 'worktree' to commit the edit instead",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        }
      ],
      "tags": [
        "json"
      ],
      "grants": [
        {
          "name": "parent-bump",
          "reason": "concluding an operation in a submodule moves the parent's gitlink, so safegit commits the parent too when commit.autoBumpParent is on",
          "kind": "proc_mutate"
        }
      ]
    },
    "cherry-pick-continue": {
      "name": "cherry-pick-continue",
      "help": "conclude a cherry-pick git stopped before committing. Every conflicted path is named with --resolve (or in a --resolve-file), and safegit writes the commit itself: one parent, the AUTHOR preserved from the commit being applied while the committer is you, git's own message draft with its comment block stripped or -m, the repository's commit-msg hook run, and the cherry-pick's state files removed afterwards. A QUEUED sequence -- `git cherry-pick <a> <b>`, which only raw git can start, since safegit's cherry-pick applies one commit -- is REFUSED, naming git's own 'cherry-pick --continue' and '--abort': the queue is part of the state a conclusion removes, so finishing one step of it would throw the rest away",
      "effect": "mutating",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "attempts": {
            "type": "integer"
          },
          "author": {
            "additionalProperties": false,
            "properties": {
              "email": {
                "type": "string"
              },
              "name": {
                "type": "string"
              }
            },
            "required": [
              "name",
              "email"
            ],
            "type": "object"
          },
          "autostash": {
            "additionalProperties": false,
            "properties": {
              "stash": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "state": {
                "type": "string"
              }
            },
            "required": [
              "state",
              "stash"
            ],
            "type": "object"
          },
          "declined_checks": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "check": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "reason": {
                  "type": "string"
                }
              },
              "required": [
                "check",
                "path",
                "reason"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "dry_run": {
            "type": "boolean"
          },
          "files": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "operation": {
            "type": "string"
          },
          "parents": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "ref": {
            "type": "string"
          },
          "residue": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "detail": {
                  "type": "string"
                },
                "step": {
                  "type": "string"
                }
              },
              "required": [
                "step",
                "detail"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "resolutions": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "choice": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "source": {
                  "type": "string"
                }
              },
              "required": [
                "path",
                "choice",
                "source"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "sha": {
            "type": [
              "string",
              "null"
            ]
          },
          "state_cleared": {
            "type": "boolean"
          },
          "tree": {
            "type": "string"
          }
        },
        "required": [
          "operation",
          "ref",
          "sha",
          "parents",
          "tree",
          "files",
          "resolutions",
          "state_cleared",
          "attempts",
          "declined_checks",
          "autostash",
          "residue",
          "dry_run",
          "author"
        ],
        "type": "object"
      },
      "flags": [
        {
          "name": "resolve",
          "help": "resolve one conflicted path, as 'path=ours|theirs|worktree|delete' (repeatable, once per path; the split is on the last '=', so a path containing one stays intact). The keywords are defined by INDEX STAGE: ours is the stage-2 blob -- your branch's current content; theirs is the stage-3 blob -- the result of applying the cherry-picked commit; worktree is the file's current content on disk; delete leaves the path out of the commit. ours and theirs also WRITE the chosen content into the working tree (git's own checkout --ours), and delete removes the file from disk (git's own rm). Paths are repository-relative. Omitted, every conflicted path is unresolved, which is a refusal listing them",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "presence": "optional"
        },
        {
          "name": "resolve-file",
          "help": "read resolutions from a TOML file of [[resolutions]] tables, each with a path and a choice key; combinable with --resolve, and a path named by both is a hard error; omitted means the --resolve flags are the whole declaration",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "m",
          "help": "commit message paragraph, replacing git's own draft; repeating it joins the values with a blank line between them; omitted means git's draft for this operation with its comment block stripped",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "short": "m",
          "presence": "optional"
        },
        {
          "name": "trailer",
          "help": "add a key-value trailer line to the commit message (repeatable)",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "presence": "optional"
        },
        {
          "name": "discard-unmatched-worktree",
          "help": "destroy working-tree content that matches no side of the conflict. A resolution that writes a stage over the file on disk, or deletes it, is refused when what is there is neither an index stage nor the content git itself wrote -- that is a hand edit, held in no commit, no stage and no stash. Passing this elects the destruction and names each file it takes. Omitted, and with --no-discard-unmatched-worktree, such a path is refused and nothing is committed; resolve it to 'worktree' to commit the edit instead",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        }
      ],
      "tags": [
        "json"
      ],
      "grants": [
        {
          "name": "parent-bump",
          "reason": "concluding an operation in a submodule moves the parent's gitlink, so safegit commits the parent too when commit.autoBumpParent is on",
          "kind": "proc_mutate"
        }
      ]
    },
    "revert-continue": {
      "name": "revert-continue",
      "help": "conclude a revert git stopped before committing. Every conflicted path is named with --resolve (or in a --resolve-file). For a SINGLE revert safegit writes the commit itself: one parent, YOU as both author and committer -- a revert is your own new change, not the reverted commit author's, which is git's own division and the opposite of what a cherry-pick does -- git's own message draft with its comment block stripped or -m, the repository's commit-msg hook run, and the revert's state files removed afterwards. Note the stage keywords: a revert applies an INVERSE patch, so theirs is what the reverted commit's parent held -- resolving to theirs keeps the revert, resolving to ours keeps the commit being reverted. A QUEUED sequence -- `git revert <a> <b>`, which only raw git can start, since safegit's revert undoes one commit -- is REFUSED, naming git's own 'revert --continue' and '--abort': the queue is part of the state a conclusion removes, so finishing one step of it would throw the rest away",
      "effect": "mutating",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "attempts": {
            "type": "integer"
          },
          "author": {
            "additionalProperties": false,
            "properties": {
              "email": {
                "type": "string"
              },
              "name": {
                "type": "string"
              }
            },
            "required": [
              "name",
              "email"
            ],
            "type": "object"
          },
          "autostash": {
            "additionalProperties": false,
            "properties": {
              "stash": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "state": {
                "type": "string"
              }
            },
            "required": [
              "state",
              "stash"
            ],
            "type": "object"
          },
          "declined_checks": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "check": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "reason": {
                  "type": "string"
                }
              },
              "required": [
                "check",
                "path",
                "reason"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "dry_run": {
            "type": "boolean"
          },
          "files": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "operation": {
            "type": "string"
          },
          "parents": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "ref": {
            "type": "string"
          },
          "residue": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "detail": {
                  "type": "string"
                },
                "step": {
                  "type": "string"
                }
              },
              "required": [
                "step",
                "detail"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "resolutions": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "choice": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "source": {
                  "type": "string"
                }
              },
              "required": [
                "path",
                "choice",
                "source"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "sha": {
            "type": [
              "string",
              "null"
            ]
          },
          "state_cleared": {
            "type": "boolean"
          },
          "tree": {
            "type": "string"
          }
        },
        "required": [
          "operation",
          "ref",
          "sha",
          "parents",
          "tree",
          "files",
          "resolutions",
          "state_cleared",
          "attempts",
          "declined_checks",
          "autostash",
          "residue",
          "dry_run",
          "author"
        ],
        "type": "object"
      },
      "flags": [
        {
          "name": "resolve",
          "help": "resolve one conflicted path, as 'path=ours|theirs|worktree|delete' (repeatable, once per path; the split is on the last '=', so a path containing one stays intact). The keywords are defined by INDEX STAGE: ours is the stage-2 blob -- your branch's current content; theirs is the stage-3 blob -- the result of UNDOING the reverted commit, which is what its parent held -- not the reverted commit's own content; worktree is the file's current content on disk; delete leaves the path out of the commit. ours and theirs also WRITE the chosen content into the working tree (git's own checkout --ours), and delete removes the file from disk (git's own rm). Paths are repository-relative. Omitted, every conflicted path is unresolved, which is a refusal listing them",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "presence": "optional"
        },
        {
          "name": "resolve-file",
          "help": "read resolutions from a TOML file of [[resolutions]] tables, each with a path and a choice key; combinable with --resolve, and a path named by both is a hard error; omitted means the --resolve flags are the whole declaration",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "m",
          "help": "commit message paragraph, replacing git's own draft; repeating it joins the values with a blank line between them; omitted means git's draft for this operation with its comment block stripped",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "short": "m",
          "presence": "optional"
        },
        {
          "name": "trailer",
          "help": "add a key-value trailer line to the commit message (repeatable)",
          "value_schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "presence": "optional"
        },
        {
          "name": "discard-unmatched-worktree",
          "help": "destroy working-tree content that matches no side of the conflict. A resolution that writes a stage over the file on disk, or deletes it, is refused when what is there is neither an index stage nor the content git itself wrote -- that is a hand edit, held in no commit, no stage and no stash. Passing this elects the destruction and names each file it takes. Omitted, and with --no-discard-unmatched-worktree, such a path is refused and nothing is committed; resolve it to 'worktree' to commit the edit instead",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        }
      ],
      "tags": [
        "json"
      ],
      "grants": [
        {
          "name": "parent-bump",
          "reason": "concluding an operation in a submodule moves the parent's gitlink, so safegit commits the parent too when commit.autoBumpParent is on",
          "kind": "proc_mutate"
        }
      ]
    },
    "switch": {
      "name": "switch",
      "help": "switch to another BRANCH, guarded twice before git runs: the worktree operation lock, held for the whole command, and then a check for uncommitted work. The command line is a deliberate subset of git's: an existing branch name, or -c to create one, and nothing else. A tag, an object name or any other commit-ish is refused, because switching onto one detaches HEAD -- the state safegit's commit, conclusion and undo paths all refuse -- and the refusal names the raw-git command for the rare deliberate case. --detach, -C, --force/--discard-changes, --orphan and --merge are refused, each naming why. There is NO file mode and no 'safegit checkout': git's checkout of files over the working tree destroys uncommitted work with no record anywhere, so safegit does not implement it at all",
      "effect": "mutating",
      "passthrough": true
    },
    "merge": {
      "name": "merge",
      "help": "merge one branch into the current one, and author the result: safegit decides the fast-forward itself and moves the ref under compare-and-swap, or runs git's merge machinery with --no-ff --no-commit and commits the staged result through its own pipeline -- so a merge safegit performed carries safegit's trailers, ran the repository's commit-msg hook and is reversible with 'safegit undo'. A merge git stops on a conflict parks, and 'safegit merge-continue' concludes it; 'safegit merge --continue' is refused and names that command. The command line is a deliberate subset of git's: exactly one branch (no octopus), no strategy selection, no --squash, no --edit and no --autostash. --no-commit computes the merge and leaves it parked even when it is clean. On an UNBORN branch -- one with no commits yet -- a merge can only be a fast-forward, so --no-ff and --no-commit are both refused there before git runs, with the reason",
      "effect": "mutating",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "attempts": {
            "type": "integer"
          },
          "declined_checks": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "check": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "reason": {
                  "type": "string"
                }
              },
              "required": [
                "check",
                "path",
                "reason"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "dry_run": {
            "type": "boolean"
          },
          "files": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "operation": {
            "type": "string"
          },
          "outcome": {
            "type": "string"
          },
          "parents": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "ref": {
            "type": "string"
          },
          "residue": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "detail": {
                  "type": "string"
                },
                "step": {
                  "type": "string"
                }
              },
              "required": [
                "step",
                "detail"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "sha": {
            "type": [
              "string",
              "null"
            ]
          },
          "state_cleared": {
            "type": "boolean"
          },
          "tree": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "operation",
          "outcome",
          "ref",
          "sha",
          "parents",
          "tree",
          "files",
          "state_cleared",
          "attempts",
          "declined_checks",
          "residue",
          "dry_run"
        ],
        "type": "object"
      },
      "passthrough": true,
      "grants": [
        {
          "name": "parent-bump",
          "reason": "merging in a submodule moves the parent's gitlink, so safegit commits the parent too when commit.autoBumpParent is on",
          "kind": "proc_mutate"
        }
      ]
    },
    "rebase": {
      "name": "rebase",
      "help": "rebase the current branch onto upstream, guarded before git runs: the worktree operation lock -- held for the whole rebase, an interactive one's editor session included, so a second safegit process in this worktree waits that long -- then a check for uncommitted work, and then a refusal to rebase over ANOTHER operation git already has in flight (a rebase over a parked revert exits 0 and strands that revert's state files behind it, blocking every later commit). That last check is kind-scoped: it refuses an in-flight state that is not a REBASE, so a rebase's own --continue, --abort and --skip pass by construction. Those three stay git's either way: safegit has no verb that finishes a rebase. A rebase on an UNBORN branch -- one with no commits yet, so with nothing to replay -- is refused before git runs too, by a separate check beside that one. The command line is a deliberate subset of git's: exactly one upstream, --onto, -i, --autostash and --rebase-merges (whose optional value is attached only). The apply backend and its patch options, --exec and --root are refused, each naming why",
      "effect": "mutating",
      "passthrough": true
    },
    "reset": {
      "name": "reset",
      "help": "reset HEAD with guards that prevent accidental data loss. The worktree operation lock is taken for EVERY reset, because every reset moves HEAD; the uncommitted-work check applies to the modes that WRITE working-tree files -- --hard, --merge and --keep -- while --soft and --mixed move only the ref and the index. Which is which is derived from safegit's git classification table, never re-read from the argument list here. The command line is a deliberate subset of git's: one of the five modes with a commit. The PATHSPEC form is refused -- it writes the shared index entry by entry, which is the one file safegit's design keeps out of -- and so is --patch",
      "effect": "mutating",
      "passthrough": true
    },
    "bisect": {
      "name": "bisect",
      "help": "binary search through commits to find a bug. The worktree operation lock is taken for EVERY invocation; the uncommitted-work check applies to the STEPPING subcommands (start, good, bad, old, new, skip, run, replay, reset), each of which checks another commit out, and not to the reporting ones (terms, log, view). Which is which is derived from safegit's git classification table, never kept as a list here -- and so is which subcommands may be typed at all: a word outside that vocabulary is refused before git runs, as is every option. 'bisect start' on an UNBORN branch is refused before git runs as well: there is no range of commits to search there",
      "effect": "mutating",
      "passthrough": true
    },
    "push": {
      "name": "push",
      "help": "push refs to remote with pre-pre-push hooks and automatic retry",
      "effect": "mutating",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "atomic": {
            "type": "boolean"
          },
          "dry_run": {
            "type": "boolean"
          },
          "force_with_lease": {
            "type": "boolean"
          },
          "pre_pre_push_hooks_run": {
            "type": "integer"
          },
          "pre_pre_push_hooks_skipped": {
            "type": [
              "string",
              "null"
            ]
          },
          "refs": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "lease": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "local_ref": {
                  "type": "string"
                },
                "local_sha": {
                  "type": "string"
                },
                "remote_ref": {
                  "type": "string"
                },
                "remote_sha": {
                  "type": [
                    "string",
                    "null"
                  ]
                }
              },
              "required": [
                "local_ref",
                "local_sha",
                "remote_ref",
                "remote_sha",
                "lease"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "remote": {
            "type": "string"
          }
        },
        "required": [
          "remote",
          "refs",
          "force_with_lease",
          "atomic",
          "pre_pre_push_hooks_run",
          "pre_pre_push_hooks_skipped",
          "dry_run"
        ],
        "type": "object"
      },
      "flags": [
        {
          "name": "pre-push-hook",
          "help": "run pre-pre-push hook scripts before pushing to remote; omitted means the hooks run. Under --dry-run they are never run whatever this says -- a hook is an arbitrary script, so running one is a mutation a preview may not perform -- and the preview says so on stderr and in the payload's pre_pre_push_hooks_skipped member. Only EXECUTION is skipped, though: DISCOVERY runs in a preview too, because it reads the filesystem and mutates nothing, and its two refusals (exit 24 for hooks left in the pre-migration .git/hooks location, exit 25 for a discovered hook without its execute bit, in either store) are verdicts about the checkout that hold whether or not anything is pushed -- a preview that skipped discovery would report success for a push that could only ever exit 24 or 25. Passing --no-pre-push-hook skips discovery along with execution",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "force-with-lease",
          "help": "force push, pinning each ref to the SHA safegit just observed on the remote (--force-with-lease=<remoteRef>:<sha>, or the empty expectation for a ref the remote does not have yet), so a ref somebody else moved in the meantime is refused rather than overwritten; forcing is consequential, so it is confirmed at the terminal and --approve-consequential answers it in advance; omitted means an ordinary push",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "refs",
          "help": "which refs to push",
          "value_schema": {
            "type": "string",
            "enum": [
              "head",
              "branches",
              "tags",
              "both"
            ]
          },
          "presence": "required",
          "choices": [
            {
              "value": "head",
              "help": "push only the current HEAD branch to the remote, ignoring other refs"
            },
            {
              "value": "branches",
              "help": "push all local branches to the remote, ignoring tags and other refs"
            },
            {
              "value": "tags",
              "help": "push all local tags to the remote without pushing any branches"
            },
            {
              "value": "both",
              "help": "push all local branches and all tags to the remote in one operation"
            }
          ]
        }
      ],
      "args": [
        {
          "name": "remote",
          "help": "name of the remote repository to push to (defaults to origin)",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ],
      "grants": [
        {
          "name": "push",
          "reason": "publishing local refs to a remote is what this command is for",
          "kind": "proc_mutate"
        },
        {
          "name": "force-push",
          "reason": "--force-with-lease overwrites the remote ref, discarding whatever the lease expectation did not cover",
          "kind": "proc_mutate"
        }
      ]
    },
    "pull": {
      "name": "pull",
      "help": "fetch from a remote and merge what was fetched, with the merge strategy stated explicitly: --merge-strategy is required and has no default, so a pull never depends on git's own configuration to decide whether it may create a merge commit. The merge step is safegit's own -- the same one 'safegit merge' performs -- so a pull that cannot fast-forward produces a commit carrying safegit's trailers, run through the repository's commit-msg hook and reversible with 'safegit undo'; a pull that can fast-forward moves the ref under compare-and-swap and puts the index and the working tree in step with it. A merge git stops on a conflict parks, and 'safegit merge-continue' concludes it. --rebase is refused and names the two commands that do it",
      "effect": "mutating",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "branch": {
            "type": [
              "string",
              "null"
            ]
          },
          "dry_run": {
            "type": "boolean"
          },
          "fetch_head": {
            "type": [
              "string",
              "null"
            ]
          },
          "merge": {
            "additionalProperties": false,
            "properties": {
              "attempts": {
                "type": "integer"
              },
              "declined_checks": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "check": {
                      "type": "string"
                    },
                    "path": {
                      "type": "string"
                    },
                    "reason": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "check",
                    "path",
                    "reason"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "dry_run": {
                "type": "boolean"
              },
              "files": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "operation": {
                "type": "string"
              },
              "outcome": {
                "type": "string"
              },
              "parents": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "ref": {
                "type": "string"
              },
              "residue": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "detail": {
                      "type": "string"
                    },
                    "step": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "step",
                    "detail"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "sha": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "state_cleared": {
                "type": "boolean"
              },
              "tree": {
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "required": [
              "operation",
              "outcome",
              "ref",
              "sha",
              "parents",
              "tree",
              "files",
              "state_cleared",
              "attempts",
              "declined_checks",
              "residue",
              "dry_run"
            ],
            "type": "object"
          },
          "operation": {
            "type": "string"
          },
          "remote": {
            "type": "string"
          }
        },
        "required": [
          "operation",
          "remote",
          "branch",
          "fetch_head",
          "merge",
          "dry_run"
        ],
        "type": "object"
      },
      "flags": [
        {
          "name": "merge-strategy",
          "help": "how the fetched commits are merged into the current branch",
          "value_schema": {
            "type": "string",
            "enum": [
              "ff",
              "ff-only",
              "no-ff"
            ]
          },
          "presence": "required",
          "choices": [
            {
              "value": "ff",
              "help": "fast-forward when possible, otherwise create a merge commit"
            },
            {
              "value": "ff-only",
              "help": "fast-forward only, refusing the pull when the branches have diverged"
            },
            {
              "value": "no-ff",
              "help": "always create a merge commit, even when a fast-forward is possible"
            }
          ]
        },
        {
          "name": "rebase",
          "help": "REFUSED, and declared so the refusal can say what to do instead: rebasing after a fetch is 'git fetch' followed by 'safegit rebase <remote>/<branch>', because a rebase is git's replay from end to end while a pull's merge step is safegit's own",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": false
        }
      ],
      "args": [
        {
          "name": "remote",
          "help": "name of the remote repository to pull from (defaults to origin)",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "branch",
          "help": "name of the remote branch to fetch and merge into the current branch",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ],
      "tags": [
        "json"
      ],
      "grants": [
        {
          "name": "parent-bump",
          "reason": "pulling in a submodule moves the parent's gitlink, so safegit commits the parent too when commit.autoBumpParent is on",
          "kind": "proc_mutate"
        }
      ]
    },
    "doctor": {
      "name": "doctor",
      "help": "run diagnostic health checks on the repository and optionally repair issues",
      "effect": "mutating",
      "flags": [
        {
          "name": "action",
          "help": "what doctor does with the health checks it runs",
          "value_schema": {
            "type": "string",
            "enum": [
              "diagnose",
              "fix",
              "uninstall"
            ]
          },
          "presence": "required",
          "choices": [
            {
              "value": "diagnose",
              "help": "run all health checks and report results without fixing any issues"
            },
            {
              "value": "fix",
              "help": "run all health checks and automatically repair any issues found"
            },
            {
              "value": "uninstall",
              "help": "remove safegit's state from this REPOSITORY -- every worktree's state directory plus the shared locks and hook store, not only the worktree you are standing in -- after listing every path it is about to remove"
            }
          ]
        }
      ]
    },
    "cherry-pick": {
      "name": "cherry-pick",
      "help": "apply ONE commit onto the current branch, and author the result: git computes the pick with --no-commit and safegit commits the staged result through its own pipeline -- so a pick safegit performed carries safegit's trailers, ran the repository's commit-msg hook and is reversible with 'safegit undo'. The AUTHOR is preserved from the commit being applied and the committer is you, which is git's own division. A pick git stops on a conflict parks -- safegit writes CHERRY_PICK_HEAD itself, because 'git cherry-pick --no-commit' does not -- and 'safegit cherry-pick-continue' concludes it; 'safegit cherry-pick --continue' is refused and names that command. The command line is a deliberate subset of git's: exactly one commit named as a commit (a range or any other revision set is refused, because a range hands the operation to git's sequencer even when it holds one commit), no --edit, no --ff, no --commit, no --cleanup, no signing and no empty-commit flags. --abort and --quit stay plain passthroughs, because they author nothing and are the way out of a state safegit must not refuse over; --no-commit is forwarded to git too, but it COMPUTES, so it takes the same refusal the restructured form does over an operation git already has in flight",
      "effect": "mutating",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "attempts": {
            "type": "integer"
          },
          "author": {
            "additionalProperties": false,
            "properties": {
              "email": {
                "type": "string"
              },
              "name": {
                "type": "string"
              }
            },
            "required": [
              "name",
              "email"
            ],
            "type": "object"
          },
          "declined_checks": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "check": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "reason": {
                  "type": "string"
                }
              },
              "required": [
                "check",
                "path",
                "reason"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "dry_run": {
            "type": "boolean"
          },
          "files": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "operation": {
            "type": "string"
          },
          "parents": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "ref": {
            "type": "string"
          },
          "residue": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "detail": {
                  "type": "string"
                },
                "step": {
                  "type": "string"
                }
              },
              "required": [
                "step",
                "detail"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "sha": {
            "type": [
              "string",
              "null"
            ]
          },
          "source": {
            "type": "string"
          },
          "state_cleared": {
            "type": "boolean"
          },
          "tree": {
            "type": "string"
          }
        },
        "required": [
          "operation",
          "source",
          "ref",
          "sha",
          "parents",
          "tree",
          "files",
          "author",
          "state_cleared",
          "attempts",
          "declined_checks",
          "residue",
          "dry_run"
        ],
        "type": "object"
      },
      "passthrough": true,
      "grants": [
        {
          "name": "parent-bump",
          "reason": "cherry-picking in a submodule moves the parent's gitlink, so safegit commits the parent too when commit.autoBumpParent is on",
          "kind": "proc_mutate"
        }
      ]
    },
    "revert": {
      "name": "revert",
      "help": "revert ONE commit by applying its inverse patch, and author the result: git computes the inverse with --no-commit and safegit commits the staged result through its own pipeline -- so a revert safegit performed carries safegit's trailers, ran the repository's commit-msg hook, is reversible with 'safegit undo', and declares the INVERSE of every move record the reverted commit declared. YOU are recorded as both author and committer, because a revert is your own new change rather than the reverted author's; that is git's own division and the opposite of what a cherry-pick does. A revert git stops on a conflict parks, and 'safegit revert-continue' concludes it; 'safegit revert --continue' is refused and names that command. The command line is a deliberate subset of git's: exactly one commit named as a commit (a range or any other revision set is refused, because a range hands the operation to git's sequencer even when it holds one commit), no --edit, no --commit, no --cleanup and no signing. --abort and --quit stay plain passthroughs, because they author nothing and are the way out of a state safegit must not refuse over; --no-commit is forwarded to git too, but it COMPUTES, so it takes the same refusal the restructured form does over an operation git already has in flight",
      "effect": "mutating",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "attempts": {
            "type": "integer"
          },
          "author": {
            "additionalProperties": false,
            "properties": {
              "email": {
                "type": "string"
              },
              "name": {
                "type": "string"
              }
            },
            "required": [
              "name",
              "email"
            ],
            "type": "object"
          },
          "declined_checks": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "check": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "reason": {
                  "type": "string"
                }
              },
              "required": [
                "check",
                "path",
                "reason"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "dry_run": {
            "type": "boolean"
          },
          "files": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "operation": {
            "type": "string"
          },
          "parents": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "ref": {
            "type": "string"
          },
          "residue": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "detail": {
                  "type": "string"
                },
                "step": {
                  "type": "string"
                }
              },
              "required": [
                "step",
                "detail"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "sha": {
            "type": [
              "string",
              "null"
            ]
          },
          "source": {
            "type": "string"
          },
          "state_cleared": {
            "type": "boolean"
          },
          "tree": {
            "type": "string"
          }
        },
        "required": [
          "operation",
          "source",
          "ref",
          "sha",
          "parents",
          "tree",
          "files",
          "author",
          "state_cleared",
          "attempts",
          "declined_checks",
          "residue",
          "dry_run"
        ],
        "type": "object"
      },
      "passthrough": true,
      "grants": [
        {
          "name": "parent-bump",
          "reason": "reverting in a submodule moves the parent's gitlink, so safegit commits the parent too when commit.autoBumpParent is on",
          "kind": "proc_mutate"
        }
      ]
    },
    "undo": {
      "name": "undo",
      "help": "reverse the last safegit-authored operation using the oplog -- a commit, an mv, an amend, a reword, a merge, pull, cherry-pick or revert safegit's own commit pipeline authored, or a conclusion (merge-continue, cherry-pick-continue, revert-continue). A fast-forward is REFUSED rather than reversed: the tip it moved onto is a commit git created and safegit never rolls a branch back over one. It moves a REF and never the working tree",
      "effect": "mutating",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "count": {
            "type": "integer"
          },
          "deleted": {
            "type": "boolean"
          },
          "dry_run": {
            "type": "boolean"
          },
          "old_sha": {
            "type": "string"
          },
          "ref": {
            "type": "string"
          },
          "residue": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "detail": {
                  "type": "string"
                },
                "step": {
                  "type": "string"
                }
              },
              "required": [
                "step",
                "detail"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "sha": {
            "type": [
              "string",
              "null"
            ]
          },
          "undone_op": {
            "type": "string"
          }
        },
        "required": [
          "ref",
          "undone_op",
          "sha",
          "old_sha",
          "count",
          "deleted",
          "residue",
          "dry_run"
        ],
        "type": "object"
      },
      "flags": [
        {
          "name": "bypass-session",
          "help": "undo across all sessions by ignoring the session ID ownership check; omitted means only this session's operations are undone",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "count",
          "help": "number of oplog operations to undo in a single invocation; omitted means one",
          "value_schema": {
            "type": "integer"
          },
          "presence": "optional"
        }
      ],
      "tags": [
        "json"
      ],
      "grants": [
        {
          "name": "parent-bump",
          "reason": "undoing a submodule commit moves the parent's gitlink back, so safegit commits the parent too when commit.autoBumpParent is on",
          "kind": "proc_mutate"
        }
      ]
    },
    "unlock": {
      "name": "unlock",
      "help": "release one of safegit's OWN lock files -- a per-ref lock, this worktree's operation lock, or the repository-wide rewrite lock -- left behind by a safegit process that was killed while holding it. It has nothing to do with git's .git/index.lock or any other lock git takes for itself. A lock whose holder is still alive is refused; ordinarily nothing needs this command, because a stale lock is reclaimed automatically by the next contender and 'safegit doctor --action fix' sweeps them, so it is the last-resort path for a filesystem where that reclamation cannot work",
      "effect": "mutating",
      "args": [
        {
          "name": "ref",
          "help": "which lock to release: a branch name (main), a full ref (refs/tags/v1), or a tool-owned lock -- safegit/rewrite for the repository-wide history-rewrite lock, safegit/operation for this worktree's operation lock",
          "value_schema": {
            "type": "string"
          },
          "presence": "required"
        }
      ]
    },
    "scan": {
      "name": "scan",
      "help": "search git history for regex pattern matches across all objects and working tree files, scanning blobs, commit messages, tag annotations, and trailers with optional scope filtering and commit range selection",
      "effect": "read_only",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "binary_skipped": {
            "type": "integer"
          },
          "blob_matches": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "commit_sha": {
                  "type": "string"
                },
                "context": {
                  "type": "string"
                },
                "in_git_dir": {
                  "type": "boolean"
                },
                "line": {
                  "type": "integer"
                },
                "object_type": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "reachable": {
                  "type": "boolean"
                },
                "sha": {
                  "type": "string"
                }
              },
              "required": [
                "object_type",
                "line",
                "reachable",
                "context"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "commit_matches": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "commit_sha": {
                  "type": "string"
                },
                "context": {
                  "type": "string"
                },
                "in_git_dir": {
                  "type": "boolean"
                },
                "line": {
                  "type": "integer"
                },
                "object_type": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "reachable": {
                  "type": "boolean"
                },
                "sha": {
                  "type": "string"
                }
              },
              "required": [
                "object_type",
                "line",
                "reachable",
                "context"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "file_matches": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "commit_sha": {
                  "type": "string"
                },
                "context": {
                  "type": "string"
                },
                "in_git_dir": {
                  "type": "boolean"
                },
                "line": {
                  "type": "integer"
                },
                "object_type": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "reachable": {
                  "type": "boolean"
                },
                "sha": {
                  "type": "string"
                }
              },
              "required": [
                "object_type",
                "line",
                "reachable",
                "context"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "objects_scanned": {
            "type": "integer"
          },
          "pattern": {
            "type": "string"
          },
          "scope": {
            "type": "string"
          },
          "tag_matches": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "commit_sha": {
                  "type": "string"
                },
                "context": {
                  "type": "string"
                },
                "in_git_dir": {
                  "type": "boolean"
                },
                "line": {
                  "type": "integer"
                },
                "object_type": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "reachable": {
                  "type": "boolean"
                },
                "sha": {
                  "type": "string"
                }
              },
              "required": [
                "object_type",
                "line",
                "reachable",
                "context"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "target": {
            "type": "string"
          },
          "total_matches": {
            "type": "integer"
          },
          "trailer_matches": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "commit_sha": {
                  "type": "string"
                },
                "context": {
                  "type": "string"
                },
                "in_git_dir": {
                  "type": "boolean"
                },
                "line": {
                  "type": "integer"
                },
                "object_type": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "reachable": {
                  "type": "boolean"
                },
                "sha": {
                  "type": "string"
                }
              },
              "required": [
                "object_type",
                "line",
                "reachable",
                "context"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "version": {
            "type": "integer"
          }
        },
        "required": [
          "version",
          "pattern",
          "objects_scanned",
          "binary_skipped",
          "total_matches",
          "blob_matches",
          "commit_matches",
          "tag_matches",
          "trailer_matches",
          "file_matches"
        ],
        "type": "object"
      },
      "flags": [
        {
          "name": "pattern",
          "help": "regular expression pattern to search for across all objects in history",
          "value_schema": {
            "type": "string"
          },
          "presence": "required"
        },
        {
          "name": "scope",
          "help": "glob pattern limiting which blob file paths are included (e.g. '*.env', 'config/**')",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "from",
          "help": "first commit hash to include when scanning history (mutually exclusive with --entire-history)",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "entire-history",
          "help": "scan all commits from the root of the repository to HEAD (mutually exclusive with --from)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "default",
          "default": false,
          "negatable": true
        },
        {
          "name": "target",
          "help": "comma-separated list of match types to include: blobs,commits,tags,trailers,files (default: all)",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ],
      "tags": [
        "json"
      ]
    },
    "version": {
      "name": "version",
      "help": "print safegit version, Go runtime version, and git version",
      "effect": "read_only",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "arch": {
            "type": "string"
          },
          "git": {
            "type": "string"
          },
          "go": {
            "type": "string"
          },
          "os": {
            "type": "string"
          },
          "safegit": {
            "type": "string"
          }
        },
        "required": [
          "safegit",
          "go",
          "os",
          "arch",
          "git"
        ],
        "type": "object"
      }
    }
  },
  "groups": {
    "backup": {
      "name": "backup",
      "help": "push, list, and restore per-branch history backups held in the tool-owned refs/backups namespace on a remote, so uncommitted-to-the-world work survives a lost machine without ever touching refs/heads",
      "commands": {
        "backup": {
          "name": "backup",
          "help": "push the current branch to its backup slot refs/backups/<branch> on the remote, after fetching that slot and refusing when it holds commits your history does not contain; the push is pinned with --force-with-lease to the exact SHA that was just observed (or to \"this ref must not exist\" for a first backup), so a concurrent backup from another machine is rejected rather than clobbered; plain git equivalent: git push --force-with-lease=refs/backups/<branch>:<observed-sha> <remote> HEAD:refs/backups/<branch>",
          "effect": "mutating",
          "flags": [
            {
              "name": "overwrite-remote-backup",
              "help": "replace a backup slot whose commits are missing from your current history, leasing on the SHA observed during this run; omitted, and with --no-overwrite-remote-backup, such a slot is a hard error because overwriting it would drop work backed up from elsewhere",
              "value_schema": {
                "type": "boolean"
              },
              "presence": "optional",
              "negatable": true
            },
            {
              "name": "allow-public-remote",
              "help": "consent to backing up to a remote that is public, or whose visibility safegit cannot determine; omitted, and with --no-allow-public-remote, such a target is a question, asked at the terminal and refused outright when there is none, because a backup pushes the whole branch and --approve-consequential says nothing about where",
              "value_schema": {
                "type": "boolean"
              },
              "presence": "optional",
              "negatable": true
            }
          ],
          "args": [
            {
              "name": "remote",
              "help": "name of the remote repository holding the backup slots (defaults to origin)",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            }
          ],
          "grants": [
            {
              "name": "push",
              "reason": "a backup slot is only useful once it is on the remote",
              "kind": "proc_mutate"
            },
            {
              "name": "force-push",
              "reason": "a backup slot is a single overwritten slot, pinned by a lease to the SHA observed a moment earlier",
              "kind": "proc_mutate"
            }
          ]
        },
        "list": {
          "name": "list",
          "help": "list every backup slot present on the remote with the branch name and the commit each slot points at, so you can see which branches are backed up from which machine before restoring one; plain git equivalent: git ls-remote <remote> 'refs/backups/*'",
          "effect": "read_only",
          "args": [
            {
              "name": "remote",
              "help": "name of the remote repository holding the backup slots (defaults to origin)",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            }
          ]
        },
        "restore": {
          "name": "restore",
          "help": "fetch the current branch's backup slot from the remote and fast-forward the branch onto it, refusing when the local branch carries commits the backup does not contain so no local work is ever discarded; plain git equivalent: git fetch <remote> refs/backups/<branch> && git merge --ff-only FETCH_HEAD",
          "effect": "mutating",
          "args": [
            {
              "name": "remote",
              "help": "name of the remote repository holding the backup slots (defaults to origin)",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            }
          ]
        }
      }
    },
    "config": {
      "name": "config",
      "help": "show, get, or set safegit configuration key-value pairs",
      "commands": {
        "show": {
          "name": "show",
          "help": "show all configuration values currently in effect for this repository, including built-in defaults and any user overrides from the .git/safegit/config.json file, printed as key-value pairs to stdout for inspection and debugging purposes",
          "effect": "read_only"
        },
        "get": {
          "name": "get",
          "help": "get the current value of a single configuration key from the .git/safegit/config.json file, printing the raw value to stdout so it can be captured by scripts or used in automation pipelines",
          "effect": "read_only",
          "args": [
            {
              "name": "key",
              "help": "the configuration key whose current value should be retrieved",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            }
          ]
        },
        "set": {
          "name": "set",
          "help": "set a configuration key to a new value in the .git/safegit/config.json file, creating the file if it does not exist yet, and persisting the change for all future safegit invocations in this repository",
          "effect": "mutating",
          "args": [
            {
              "name": "key",
              "help": "the configuration key to set to the specified value in config.json",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "value",
              "help": "the new value to assign to the specified configuration key",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            }
          ]
        }
      }
    },
    "hook": {
      "name": "hook",
      "help": "manage pre-pre-push hook scripts that run before every push",
      "commands": {
        "list": {
          "name": "list",
          "help": "list every pre-pre-push hook location safegit knows about, with its origin, its path and whether it is executable, so you can audit which checks run before every push. Three origins are shown: local, the tool-owned live store under the repository's common .git/safegit/hooks that hook install writes to and every worktree shares; tracked, the hooks the CHECKOUT provides in .safegit/hooks, which run because they are in that directory whether or not git tracks them, so cloning a repository and pushing from that checkout runs the repository's scripts; and legacy, the pre-migration location in git's own .git/hooks, which nothing runs any more and safegit hook migrate relocates. Non-executable and non-hook entries are listed too, because the hook an operator is asking about is usually the one that is NOT running",
          "effect": "read_only"
        },
        "run": {
          "name": "run",
          "help": "run all installed pre-pre-push hooks (or a single named hook) immediately without performing an actual push, so you can verify that all configured hooks pass before committing to a real push operation. Discovery's own verdicts about the checkout reach here too: exit 24 while a hook is still in the pre-migration .git/hooks location, and exit 25 when a discovered hook is not executable, in EITHER store -- never a silent skip and never a 'no hooks to run', because a command whose whole purpose is to say whether the checks pass must not exit 0 because a check was passed over",
          "effect": "mutating",
          "dry_run_supported": false,
          "dry_run_unsupported_reason": "running a hook executes an operator-supplied script whose effects safegit cannot know in advance, so there is nothing honest to preview; run 'safegit hook list' to see which scripts would run",
          "args": [
            {
              "name": "name",
              "help": "name of a specific hook to run; omit to run all installed hooks",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            }
          ]
        },
        "install": {
          "name": "install",
          "help": "install a pre-pre-push hook by copying a script file into the live store under the repository's common .git/safegit/hooks directory and making it executable, so that safegit push runs it before any network I/O occurs. The store is keyed on the common git dir, so a hook installed from a linked worktree is the same hook every worktree of the repository runs. An existing destination is refused rather than overwritten",
          "effect": "mutating",
          "args": [
            {
              "name": "path",
              "help": "filesystem path to the hook script file to install into safegit",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            }
          ]
        },
        "remove": {
          "name": "remove",
          "help": "remove one hook from the tool-owned live store under the repository's common .git/safegit/hooks directory by name, naming either the store-relative path such as pre-pre-push.d/20-lint or just the base name, so a hook can be retired or replaced without deleting files by hand; a name that resolves only to a hook the checkout provides in .safegit/hooks is refused, because removing that one means deleting the file and committing that, and a name carried by both stores removes the live one and says the other still runs",
          "effect": "mutating",
          "args": [
            {
              "name": "name",
              "help": "name of the installed hook to remove, as shown by 'safegit hook list'",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            }
          ]
        },
        "migrate": {
          "name": "migrate",
          "help": "move safegit's hooks out of git's own .git/hooks directory into the tool-owned .git/safegit/hooks store, relocating the pre-pre-push file and the pre-pre-push.d directory unconditionally because those two names are the only ones safegit ever wrote there, and reporting success with an explanation when there is nothing to move. Both ends are under the repository's COMMON git dir, which is git's own hook directory in every worktree, so migration run from a linked worktree relocates the repository's hooks",
          "effect": "mutating"
        }
      }
    },
    "author": {
      "name": "author",
      "help": "audit and rewrite commit author/committer identity — list all identities, check against expected values, and rewrite name or email across history",
      "commands": {
        "list": {
          "name": "list",
          "help": "list all distinct author and committer identities across the entire commit history, showing name, email, role, and commit count for each unique identity — useful for auditing repositories with multiple contributors or detecting unwanted identity variations such as typos, old email addresses, or bot accounts that should be consolidated before a rewrite",
          "effect": "read_only",
          "payload_schema": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "count": {
                  "type": "integer"
                },
                "email": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "role": {
                  "enum": [
                    "author",
                    "committer",
                    "both"
                  ]
                }
              },
              "required": [
                "name",
                "email",
                "role",
                "count"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "tags": [
            "json"
          ]
        },
        "check": {
          "name": "check",
          "help": "check that all commits use the expected author and committer identity by scanning every commit in the repository history, reporting any deviations with the exact commit hashes and mismatched fields, and suggesting the corresponding safegit author rewrite command to fix each deviation found",
          "effect": "read_only",
          "payload_schema": {
            "additionalProperties": false,
            "properties": {
              "deviations": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "author_email": {
                      "type": "string"
                    },
                    "author_name": {
                      "type": "string"
                    },
                    "committer_email": {
                      "type": "string"
                    },
                    "committer_name": {
                      "type": "string"
                    },
                    "sha": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "sha",
                    "author_name",
                    "author_email",
                    "committer_name",
                    "committer_email"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "expected": {
                "additionalProperties": false,
                "properties": {
                  "email": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  }
                },
                "type": "object"
              }
            },
            "required": [
              "expected",
              "deviations"
            ],
            "type": "object"
          },
          "flags": [
            {
              "name": "name",
              "help": "expected author and committer display name that all commits should use",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "email",
              "help": "expected author and committer email address that all commits should use",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            }
          ],
          "tags": [
            "json"
          ]
        },
        "rewrite": {
          "name": "rewrite",
          "help": "rewrite author and committer name or email across all commit history using git filter-branch style rewriting, replacing every occurrence of the old identity with the new one in both author and committer fields while preserving timestamps, commit messages, tree contents, and parent relationships so the rewritten history is otherwise identical to the original",
          "effect": "mutating",
          "consequential": true,
          "payload_schema": {
            "additionalProperties": false,
            "properties": {
              "commits_matched": {
                "type": "integer"
              },
              "commits_rewritten": {
                "type": "integer"
              },
              "commits_to_check": {
                "type": "integer"
              },
              "dry_run": {
                "type": "boolean"
              },
              "name_changed": {
                "type": "integer"
              },
              "new_email": {
                "type": "string"
              },
              "new_head": {
                "type": "string"
              },
              "new_name": {
                "type": "string"
              },
              "old_email": {
                "type": "string"
              },
              "old_head": {
                "type": "string"
              },
              "old_name": {
                "type": "string"
              },
              "parent_only": {
                "type": "integer"
              },
              "rewrites": {
                "additionalProperties": {
                  "type": "string"
                },
                "type": "object"
              },
              "sync_skipped": {
                "type": "boolean"
              },
              "tags": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "annotated": {
                      "type": "boolean"
                    },
                    "new_sha": {
                      "type": "string"
                    },
                    "old_sha": {
                      "type": "string"
                    },
                    "refname": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "refname",
                    "old_sha",
                    "new_sha",
                    "annotated"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "version": {
                "type": "integer"
              }
            },
            "required": [
              "version",
              "dry_run"
            ],
            "type": "object"
          },
          "flags": [
            {
              "name": "old-name",
              "help": "current author or committer display name to search for and replace",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "new-name",
              "help": "new display name to substitute wherever the old name is found in history",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "old-email",
              "help": "current author or committer email address to search for and replace",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "new-email",
              "help": "new email address to substitute wherever the old email is found in history",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            }
          ],
          "tags": [
            "json"
          ],
          "constraints": [
            {
              "type": "all_or_none",
              "name": "author-name",
              "members": [
                {
                  "kind": "flag",
                  "name": "old-name",
                  "when": "present"
                },
                {
                  "kind": "flag",
                  "name": "new-name",
                  "when": "present"
                }
              ]
            },
            {
              "type": "all_or_none",
              "name": "author-email",
              "members": [
                {
                  "kind": "flag",
                  "name": "old-email",
                  "when": "present"
                },
                {
                  "kind": "flag",
                  "name": "new-email",
                  "when": "present"
                }
              ]
            },
            {
              "type": "at_least_one",
              "name": "author-change",
              "members": [
                {
                  "kind": "constraint",
                  "name": "author-name"
                },
                {
                  "kind": "constraint",
                  "name": "author-email"
                }
              ]
            }
          ]
        }
      }
    },
    "scrub": {
      "name": "scrub",
      "help": "surgically rewrite git history to remove or replace sensitive content: file and match rewrite the commits, trees and blobs of a range the caller selects (--from or --entire-history), run applies a recipe of such operations in one coordinated pass, and verify only reads -- it confirms that the patterns named on its command line are absent from the whole object store",
      "commands": {
        "file": {
          "name": "file",
          "help": "replace or remove a specific file across every commit in a SELECTED RANGE of history -- --from <commit> or --entire-history, one of which is required -- rewriting each affected commit tree to either substitute the file's contents with those of a sanitized file or delete it entirely from every snapshot in that range. A --delete also removes the move records naming that path, whole, since the path they refer to is being erased; a --replace-with edits no message, because the path still exists and a record naming it is still true",
          "effect": "mutating",
          "consequential": true,
          "payload_schema": {
            "additionalProperties": false,
            "properties": {
              "cleanup_errors": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "cleanup_ok": {
                "type": "boolean"
              },
              "commit_count": {
                "type": "integer"
              },
              "commits_rewritten": {
                "type": "integer"
              },
              "dry_run": {
                "type": "boolean"
              },
              "file": {
                "type": "string"
              },
              "from": {
                "type": "string"
              },
              "messages_modified": {
                "type": "integer"
              },
              "mode": {
                "enum": [
                  "replace",
                  "remove"
                ]
              },
              "new_blob_sha": {
                "type": "string"
              },
              "new_head": {
                "type": "string"
              },
              "old_head": {
                "type": "string"
              },
              "pre_rewrite_remotes": {
                "additionalProperties": {
                  "type": "string"
                },
                "type": "object"
              },
              "range": {
                "enum": [
                  "range",
                  "entire_history"
                ]
              },
              "rewrites": {
                "additionalProperties": {
                  "type": "string"
                },
                "type": "object"
              },
              "sync_skipped": {
                "type": "boolean"
              },
              "tags": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "annotated": {
                      "type": "boolean"
                    },
                    "new_sha": {
                      "type": "string"
                    },
                    "old_sha": {
                      "type": "string"
                    },
                    "refname": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "refname",
                    "old_sha",
                    "new_sha",
                    "annotated"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "version": {
                "type": "integer"
              }
            },
            "required": [
              "version",
              "dry_run",
              "file",
              "mode",
              "range",
              "from",
              "commit_count",
              "old_head"
            ],
            "type": "object"
          },
          "flags": [
            {
              "name": "reason",
              "help": "mandatory audit trail message explaining why this scrub operation is needed",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "remap-shas-in",
              "help": "glob selecting files whose full 40-character commit hashes are remapped to the rewritten SHAs during the walk, keeping hash-referencing files like JSONL changelogs self-consistent at every commit (repeatable; same matching semantics as --scope; not applied inside submodule histories)",
              "value_schema": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "presence": "optional",
              "unique": true
            },
            {
              "name": "mode",
              "help": "what happens to the file at every commit in range",
              "presence": "required",
              "choices": [
                {
                  "name": "delete",
                  "help": "delete the file from every commit in range"
                },
                {
                  "name": "replace-with",
                  "help": "replace the file's contents with those of a sanitized file",
                  "flags": [
                    {
                      "name": "value",
                      "help": "path to the file whose contents replace the target in every commit; resolved against YOUR current directory, unlike the target argument, which is repository-relative",
                      "value_schema": {
                        "type": "string"
                      },
                      "presence": "required"
                    }
                  ]
                }
              ],
              "elect_by": "member-flags"
            },
            {
              "name": "range",
              "help": "how much of the history is rewritten",
              "presence": "required",
              "choices": [
                {
                  "name": "from",
                  "help": "rewrite the commits from a given commit forward",
                  "flags": [
                    {
                      "name": "value",
                      "help": "first commit hash to include when rewriting history",
                      "value_schema": {
                        "type": "string"
                      },
                      "presence": "required"
                    }
                  ]
                },
                {
                  "name": "entire-history",
                  "help": "rewrite every commit from the root of the repository to HEAD"
                }
              ],
              "elect_by": "member-flags"
            }
          ],
          "args": [
            {
              "name": "file",
              "help": "repository-relative path to the file that should be scrubbed from history",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            }
          ],
          "tags": [
            "json"
          ]
        },
        "match": {
          "name": "match",
          "help": "replace every occurrence of a regex pattern in the blobs, commit messages and tag annotations of a SELECTED RANGE of history -- --from <sha> or --entire-history, one of which is required -- rewriting commit trees so that sensitive values like secrets and credentials are removed from every snapshot in that range. A move record is rewritten as a record rather than as text: the substitution applies to the decoded paths and the pair is re-encoded, so the output always parses, a pattern written against the escaped spelling matches nothing, and a substitution whose result would no longer be a move is refused before any ref moves",
          "effect": "mutating",
          "consequential": true,
          "payload_schema": {
            "additionalProperties": false,
            "properties": {
              "binary_skipped": {
                "type": "integer"
              },
              "blob_matches": {
                "type": "integer"
              },
              "blobs_replaced": {
                "type": "integer"
              },
              "cleanup_errors": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "cleanup_ok": {
                "type": "boolean"
              },
              "commit_matches": {
                "type": "integer"
              },
              "commits_rewritten": {
                "type": "integer"
              },
              "dry_run": {
                "type": "boolean"
              },
              "estimated_commits": {
                "type": "integer"
              },
              "file_matches": {
                "type": "integer"
              },
              "messages_modified": {
                "type": "integer"
              },
              "new_head": {
                "type": "string"
              },
              "objects_matched": {
                "type": "integer"
              },
              "objects_scanned": {
                "type": "integer"
              },
              "old_head": {
                "type": "string"
              },
              "pattern": {
                "type": "string"
              },
              "pre_rewrite_remotes": {
                "additionalProperties": {
                  "type": "string"
                },
                "type": "object"
              },
              "rewrites": {
                "additionalProperties": {
                  "type": "string"
                },
                "type": "object"
              },
              "scope": {
                "enum": [
                  "range",
                  "entire_history"
                ]
              },
              "scope_filter": {
                "type": "string"
              },
              "submodule_pre_rewrite_remotes": {
                "additionalProperties": {
                  "additionalProperties": {
                    "type": "string"
                  },
                  "type": "object"
                },
                "type": "object"
              },
              "sync_skipped": {
                "type": "boolean"
              },
              "tag_matches": {
                "type": "integer"
              },
              "tags": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "annotated": {
                      "type": "boolean"
                    },
                    "new_sha": {
                      "type": "string"
                    },
                    "old_sha": {
                      "type": "string"
                    },
                    "refname": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "refname",
                    "old_sha",
                    "new_sha",
                    "annotated"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "tags_rewritten": {
                "type": "integer"
              },
              "total_matches": {
                "type": "integer"
              },
              "version": {
                "type": "integer"
              }
            },
            "required": [
              "version",
              "dry_run",
              "pattern"
            ],
            "type": "object"
          },
          "flags": [
            {
              "name": "pattern",
              "help": "regular expression pattern to search for across all blobs in history",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "reason",
              "help": "mandatory audit trail message explaining why this scrub operation is needed",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "scope",
              "help": "glob pattern limiting which file paths are searched (e.g. '*.env', 'config/**')",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "remap-shas-in",
              "help": "glob selecting files whose full 40-character commit hashes are remapped to the rewritten SHAs during the walk, keeping hash-referencing files like JSONL changelogs self-consistent at every commit (repeatable; same matching semantics as --scope; not applied inside submodule histories)",
              "value_schema": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "presence": "optional",
              "unique": true
            },
            {
              "name": "substitution",
              "help": "what replaces each match",
              "presence": "required",
              "choices": [
                {
                  "name": "replace",
                  "help": "substitute a literal string for every match",
                  "flags": [
                    {
                      "name": "value",
                      "help": "literal string to substitute for each regex match found in history",
                      "value_schema": {
                        "type": "string"
                      },
                      "presence": "required"
                    }
                  ]
                },
                {
                  "name": "mangle",
                  "help": "substitute random printable ASCII of the same length for every match"
                }
              ],
              "elect_by": "member-flags"
            },
            {
              "name": "range",
              "help": "how much of the history is rewritten",
              "presence": "required",
              "choices": [
                {
                  "name": "from",
                  "help": "rewrite the commits from a given commit forward",
                  "flags": [
                    {
                      "name": "value",
                      "help": "first commit hash to include when rewriting history",
                      "value_schema": {
                        "type": "string"
                      },
                      "presence": "required"
                    }
                  ]
                },
                {
                  "name": "entire-history",
                  "help": "rewrite every commit from the root of the repository to HEAD"
                }
              ],
              "elect_by": "member-flags"
            }
          ],
          "tags": [
            "json"
          ]
        },
        "run": {
          "name": "run",
          "help": "execute a multi-operation scrub recipe from a TOML file, applying all pattern replacements and file removals across history in a single coordinated pass with topological commit ordering, overlap detection between operations, and automatic verification that no matched content survives in the rewritten object store — use --diff to preview all changes as unified diffs before committing to the rewrite",
          "effect": "mutating",
          "consequential": true,
          "payload_schema": {
            "additionalProperties": false,
            "properties": {
              "binary_skipped": {
                "type": "integer"
              },
              "blob_diffs": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "diff": {
                      "type": "string"
                    },
                    "new_sha": {
                      "type": "string"
                    },
                    "old_sha": {
                      "type": "string"
                    },
                    "paths": {
                      "items": {
                        "type": "string"
                      },
                      "type": "array"
                    }
                  },
                  "required": [
                    "old_sha",
                    "paths",
                    "diff"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "blobs_replaced": {
                "type": "integer"
              },
              "cleanup_errors": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "cleanup_ok": {
                "type": "boolean"
              },
              "commits_rewritten": {
                "type": "integer"
              },
              "diff": {
                "type": "boolean"
              },
              "dry_run": {
                "type": "boolean"
              },
              "estimated_commits": {
                "type": "integer"
              },
              "message_diffs": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "commit_sha": {
                      "type": "string"
                    },
                    "new_message": {
                      "type": "string"
                    },
                    "old_message": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "commit_sha",
                    "old_message",
                    "new_message"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "messages_modified": {
                "type": "integer"
              },
              "new_head": {
                "type": "string"
              },
              "objects_scanned": {
                "type": "integer"
              },
              "old_head": {
                "type": "string"
              },
              "operation_count": {
                "type": "integer"
              },
              "operations": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "affected_files": {
                      "items": {
                        "type": "string"
                      },
                      "type": "array"
                    },
                    "blob_matches": {
                      "type": "integer"
                    },
                    "commit_matches": {
                      "type": "integer"
                    },
                    "index": {
                      "type": "integer"
                    },
                    "pattern": {
                      "type": "string"
                    },
                    "tag_matches": {
                      "type": "integer"
                    }
                  },
                  "required": [
                    "index",
                    "pattern",
                    "blob_matches",
                    "commit_matches",
                    "tag_matches",
                    "affected_files"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "pre_rewrite_remotes": {
                "additionalProperties": {
                  "type": "string"
                },
                "type": "object"
              },
              "rewrites": {
                "additionalProperties": {
                  "type": "string"
                },
                "type": "object"
              },
              "shown": {
                "type": "integer"
              },
              "sync_skipped": {
                "type": "boolean"
              },
              "tags": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "annotated": {
                      "type": "boolean"
                    },
                    "new_sha": {
                      "type": "string"
                    },
                    "old_sha": {
                      "type": "string"
                    },
                    "refname": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "refname",
                    "old_sha",
                    "new_sha",
                    "annotated"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "tags_rewritten": {
                "type": "integer"
              },
              "total_affected_files": {
                "type": "integer"
              },
              "total_blob_matches": {
                "type": "integer"
              },
              "total_blobs": {
                "type": "integer"
              },
              "total_commit_matches": {
                "type": "integer"
              },
              "total_tag_matches": {
                "type": "integer"
              },
              "truncated": {
                "type": "boolean"
              },
              "version": {
                "type": "integer"
              }
            },
            "required": [
              "version",
              "dry_run",
              "operation_count"
            ],
            "type": "object"
          },
          "flags": [
            {
              "name": "reason",
              "help": "mandatory audit trail message explaining why this scrub operation is needed",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "diff",
              "help": "preview what would change without modifying any objects, showing unified diffs; omitted means the rewrite is performed",
              "value_schema": {
                "type": "boolean"
              },
              "presence": "optional",
              "negatable": true
            },
            {
              "name": "limit",
              "help": "maximum number of blob diffs to show in --diff mode; omitted means 50",
              "value_schema": {
                "type": "integer"
              },
              "presence": "optional"
            },
            {
              "name": "remap-shas-in",
              "help": "glob selecting files whose full 40-character commit hashes are remapped to the rewritten SHAs during the walk, keeping hash-referencing files like JSONL changelogs self-consistent at every commit (repeatable; same matching semantics as --scope; not applied inside submodule histories)",
              "value_schema": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "presence": "optional",
              "unique": true
            },
            {
              "name": "range",
              "help": "how much of the history is rewritten",
              "presence": "required",
              "choices": [
                {
                  "name": "from",
                  "help": "rewrite the commits from a given commit forward",
                  "flags": [
                    {
                      "name": "value",
                      "help": "first commit hash to include when rewriting history",
                      "value_schema": {
                        "type": "string"
                      },
                      "presence": "required"
                    }
                  ]
                },
                {
                  "name": "entire-history",
                  "help": "rewrite every commit from the root of the repository to HEAD"
                }
              ],
              "elect_by": "member-flags"
            }
          ],
          "args": [
            {
              "name": "recipe",
              "help": "path to the TOML recipe file containing scrub operations",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            }
          ],
          "tags": [
            "json"
          ]
        },
        "verify": {
          "name": "verify",
          "help": "confirm that the patterns named on the command line -- repeatable --pattern regexes, the operations of a scrub recipe file, or both -- are absent from every object in the git object store, scanning blobs, commit messages, and tag annotations and reporting detailed per-pattern pass or fail results with match locations for any violations found",
          "effect": "read_only",
          "payload_schema": {
            "additionalProperties": false,
            "properties": {
              "failed": {
                "type": "integer"
              },
              "passed": {
                "type": "integer"
              },
              "patterns": {
                "type": "integer"
              },
              "results": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "details": {
                      "items": {
                        "type": "string"
                      },
                      "type": "array"
                    },
                    "pass": {
                      "type": "boolean"
                    },
                    "pattern": {
                      "type": "string"
                    },
                    "scope": {
                      "type": "string"
                    },
                    "source": {
                      "enum": [
                        "flag",
                        "recipe"
                      ]
                    }
                  },
                  "required": [
                    "pattern",
                    "source",
                    "pass"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "version": {
                "type": "integer"
              }
            },
            "required": [
              "version",
              "patterns",
              "passed",
              "failed",
              "results"
            ],
            "type": "object"
          },
          "flags": [
            {
              "name": "pattern",
              "help": "regular expression that must be absent from every object in the repository (repeatable)",
              "value_schema": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "presence": "optional",
              "unique": true
            },
            {
              "name": "scope",
              "help": "glob pattern limiting which blob file paths a --pattern match counts against (e.g. '*.env', 'config/**'); recipe operations carry their own scope in the recipe file",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            }
          ],
          "args": [
            {
              "name": "recipe",
              "help": "path to a scrub recipe TOML file whose operations' patterns are verified; the format is the one 'scrub run' takes, and its replace/mangle/depends_on fields are ignored here because verification substitutes nothing",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            }
          ],
          "tags": [
            "json"
          ],
          "constraints": [
            {
              "type": "at_least_one",
              "name": "verify-input",
              "members": [
                {
                  "kind": "flag",
                  "name": "pattern",
                  "when": "present"
                },
                {
                  "kind": "arg",
                  "name": "recipe",
                  "when": "present"
                }
              ]
            },
            {
              "type": "requires",
              "name": "verify-scope",
              "flag": "scope",
              "depends_on": "pattern"
            }
          ]
        }
      }
    }
  },
  "deprecated": {
    "rewrite-author": "use 'safegit author rewrite' instead"
  },
  "infra": {
    "handshakes": [
      {
        "env_var": "CLAUDE_CODE_SESSION_ID",
        "help": "Claude Code session identifier set by the invoking agent session; scopes 'safegit undo' to operations this session performed and is recorded as a commit trailer"
      }
    ]
  }
}
